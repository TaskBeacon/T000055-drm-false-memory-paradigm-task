# Task Plot Prompt

Create a clean scientific task-flow timeline diagram only.

Leave the top 25% of the image blank for a later fixed header, title, subtitle, and logo overlay. Do not draw any title, subtitle, logo, watermark, or branding.

Task: DRM False Memory Paradigm.

Draw two horizontal timeline rows:

1. DRM list block
2. Recognition item types

Use compact screen thumbnails connected by arrows. Use a white background, crisp dark labels, and restrained accent colors. Make row labels left aligned. Keep text legible and inside the timeline. Use loop brackets for repeated words/items.

DRM list block row phases:

- Block intro: list label, wait for `SPACE`.
- Study word, 1250 ms: one centered semantic associate word.
- Study gap, 350 ms: blank screen.
- Loop note: repeat study word + gap for 10 study words.
- Recognition instructions: confidence scale reminder, wait for `SPACE`.
- Recognition item, 2000 ms: test word plus scale `1 sure new`, `2 probably new`, `3 probably old`, `4 sure old`.
- Loop note: repeat recognition item for 10 test words.
- Block summary: studied old rate, critical-lure old rate, foil old rate, wait for `SPACE`.

Recognition item types row:

- Studied words: old response should be 3 or 4.
- Critical lure: nonpresented theme word; old response is false memory.
- Foils: unrelated new words; new response should be 1 or 2.

Add a small note: six themes bread / cold / doctor / fruit / sleep / sweet. Recognition mix: 5 studied + 1 critical lure + 4 foils per block.

Do not include file names or implementation details.
